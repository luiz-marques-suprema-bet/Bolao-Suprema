.DS_Store
.claude/
node_modules/
dist/
coverage/
*.log

# Environment — never commit real credentials
.env
.env.*
!.env.example

# Uploads — user media should live in Supabase Storage, not in the repo
uploads/

# Pick backups — production user data, never commit
backups/

# Local QA/artifact output
tmp-e2e/
speedtest-*-output/
stream-diagnostic-output/
yt-buffer-test-output/
supabase/.temp/
